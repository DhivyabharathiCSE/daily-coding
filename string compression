string compression

#include<iostream>
#include<string>
using namespace std;
int main()
{
string s;
cin>>s;

string r=" ";
int c=1;
for(int i=1;i<=s.length();i++)
{
if(i<s.length()&&s.at(i)==s.at(i-1)
{
c++;
}
else
{
r=r+s.at(i-1);
r+=to_string(c);
c=1;
}
}
cout<<r;
}
